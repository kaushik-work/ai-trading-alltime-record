events {}

http {

    # Redirect HTTP → HTTPS
    server {
        listen 80;
        server_name thegaintcompany.com;
        return 301 https://$host$request_uri;
    }

    server {
        listen 443 ssl;
        server_name thegaintcompany.com;

        # SSL certificates — mounted from /etc/letsencrypt on the host
        ssl_certificate     /etc/letsencrypt/live/thegaintcompany.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/thegaintcompany.com/privkey.pem;

        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         HIGH:!aNULL:!MD5;

        # FastAPI REST endpoints
        location / {
            proxy_pass         http://api:8000;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }

        # WebSocket endpoint (/ws) — needs Upgrade headers
        location /ws {
            proxy_pass         http://api:8000/ws;
            proxy_http_version 1.1;
            proxy_set_header   Upgrade $http_upgrade;
            proxy_set_header   Connection "upgrade";
            proxy_set_header   Host $host;
            proxy_read_timeout 3600s;
        }
    }
}
